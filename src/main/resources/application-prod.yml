spring:
  application:
    name: board
  datasource:
    url: jdbc:mysql://mysql:3306/devopsBoard?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: ${username}
    password: ${password}
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
server:
  port: 8080
